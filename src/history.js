//history package is a dependency of react router dom
import { createBrowserHistory as createHistory } from 'history';
// import createHistory from 'history/createBrowserHistory';

export default createHistory();
